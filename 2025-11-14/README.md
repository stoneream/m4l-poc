## 概要

LiveのデバイスへM4L経由でパラメーターをセットすることができるか？を検証する。

## 結論

できる

## 補足

デバイスおよびパラメーターの走査はO(n)でやってしまっても問題ないような気がする。  
（むしろメニューの状態を管理する実装のほうがバグを混入するおそれがある。）

マスターとしてパラメーターをセットするデバイスなのであれば、  
双方向にパラメーターを同期させることは一旦考慮しなくても良いのではないか、と思う。
